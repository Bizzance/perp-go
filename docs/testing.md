# 测试

三层测试，用构建标签分开：

| 层     | 怎么跑                  | 依赖                              | 测什么                                                                 |
|--------|-------------------------|-----------------------------------|------------------------------------------------------------------------|
| 单元   | `make test`             | 无                                | 纯逻辑：撮合订单簿、签名/鉴权、参数解析、JSON格式、配置校验            |
| 集成   | `make test-integration` | docker（自动拉容器）              | 碰数据库/Redis的逻辑：仓库的事务、引擎撮合前的检查、接口的完整处理链路 |
| 端到端 | `make test-e2e`         | docker compose（整套系统含Kafka） | 通过对外接口和WebSocket验证全链路，发布前跑，不放进日常测试            |

普通的`go test ./...`只跑单元测试，速度快、不需要任何外部依赖。集成测试文件顶部有
`//go:build integration`，不加标签编译不进去。

## 跑集成测试

```bash
make test-integration                                   # 全部，约15秒
make test-integration ARGS='-v ./internal/service/'     # 只跑一个包，ARGS原样传给go test
make test-integration ARGS='-v -run TestEngineFreeze ./internal/service/'
```

`scripts/test-integration.sh`做的事：起一个一次性的MySQL 8.4和Redis 7容器（端口随机、只绑
`127.0.0.1`），等它们真的能接连接，设好环境变量，跑`go test -tags=integration -count=1 -p 1`，
结束后不管成败都删掉容器。需要docker和go，不需要提前建库。

直接`go test -tags=integration`而没设环境变量时测试会 **失败**并提示用`make test-integration`，
不会悄悄跳过——标了integration就是明确要跑，"跳过却显示通过"会给人错误的安心。

| 环境变量               | 含义                                   |
|------------------------|----------------------------------------|
| `PERP_TEST_MYSQL_DSN`  | 管理员DSN，不带库名，要有建库/删库权限 |
| `PERP_TEST_REDIS_ADDR` | Redis地址                              |
| `PERP_TEST_REDIS_PASS` | Redis密码，没有就不设                  |

## 跑端到端测试

```bash
make test-e2e                                   # 约1.5分钟(镜像有缓存时)，冷启动2到3分钟
E2E_KEEP=1 make test-e2e                        # 跑完不拆环境，排查用；脚本会打印清理命令
make test-e2e ARGS='-count=1 -v -run TestE2E_03'
```

`scripts/e2e.sh`用docker compose拉起一整套一次性的系统（contract-api、contract-engine、MySQL、Redis、Kafka，
随机端口、随机密码、一把`trade|ops`权限的密钥，只绑`127.0.0.1`），等两个服务健康，设好环境变量，跑带`e2e`标签的
`e2e/`包，结束后不管成败都拆掉。失败时会打印api和engine最近的日志。测试用的签名是生产代码里的
`api.SignRequest`，跟合作方对接用的是同一套。

集成测试不起Kafka、直接调服务，覆盖不到消费者订阅、topic发现、重启后消费者是否还活着这类基础设施层的问题，
部署验证时手工发现过几个，端到端测试把那套手工流程固化下来：

| 用例 | 验证什么                                                                                                                                                                |
|------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 01   | 两个服务健康，设置指数价                                                                                                                                                |
| 02   | 没签名、签名不对被挡在外面                                                                                                                                              |
| 03   | 下单 → Kafka → 引擎撮合 → 双方结算，余额/仓位保证金/权益的数值                                                                                 |
| 04   | 撤单事件经Kafka到引擎，摘单并退回冻结保证金                                                                                                    |
| 05   | 冻结账户：开仓被拒、存量开仓挂单经Kafka被清理、解冻后恢复                                                                                                               |
| 06   | 结束本轮事件经Kafka到引擎，`round`推进、信用额度清零                                                                                                                    |
| 07   | WebSocket：未签名握手被拒；订阅后下单引起的深度变化和账户快照实时推送                                                                                                   |
| 08   | 市价买入吃掉高于标记价的卖单                                                                                                                                            |
| 09   | 下单、撤单接口不被Kafka批处理拖慢：连续5次取最快的一次，不超过500ms（kafka-go默认的1秒批处理超时会让每次都要1秒以上） |
| 99   | 重启引擎：订单簿从数据库恢复出挂单，重启后的消费者还活着，新下的单能成交；重启整体耗时不超过12秒（kafka-go拉取的`MaxWait`回退到默认10秒会让关闭要15到26秒，这里能抓到） |

用例之间有顺序依赖：99会重启引擎，必须最后跑；前面的用例会在订单簿里留下一些买单（64111、64000），撮合是价格
优先，所以99的买单挂得比它们都高才能保证卖单吃到的是这一笔（这个坑第一次跑时踩到过：卖单成交在了遗留的买单上，
看起来像"重启后消费者没工作"，其实是测试自己的价格没排对）。

没有覆盖强平（要把标记价格推到极端位置）和资金费率（结算周期8小时），这两块靠集成测试。

变异检查：让撤单消费者订阅一个不存在的topic，用例04和05失败；重启时不恢复订单簿，用例99失败；去掉Kafka生产者的
`BatchTimeout`（kafka-go默认1秒，每笔下单撤单固定多1秒），用例09报"下单最快的一次也要1.03秒"失败。这个延迟之前一直没暴露，
因为轮询超时都放得很宽，是模拟客户端页面上下单要等一秒才有反应才发现的，见 [sim-client.md](sim-client.md)。

## 隔离方式

- **MySQL：每个测试一个独立的库**。`testutil.NewDB(t)`建一个随机名字的库，执行`sql/schema.sql`
  （去掉里面的`CREATE DATABASE`/`USE`），返回连接，测试结束自动删库。所以测试跑的表结构和种子数据
  就是生产用的那份，改了`schema.sql`测试自动跟着变；测试之间互不影响，可以放心写任意脏数据。
- **Redis：共用一个实例，靠随机uid隔离**。锁、nonce这类键里都带uid，`testutil.UIDBase()`给每个
  测试一段随机的大uid，同一个测试里的多个账户用它加偏移。标记价格这类按symbol的键是共享的，
  所以不同包的测试用`-p 1`串行，同一个包里的集成测试不要用`t.Parallel()`。
- **Kafka：不起**。API层的`Server.producer`是个小接口（`eventPublisher`），测试里换成
  `fakePublisher`，只记录发了哪些事件、可以让它模拟失败。引擎服务本来就是直接调用，不经过Kafka。

## 测试夹具

- `internal/testutil`：`NewDB`、`NewCache`、`UIDBase`。
- `internal/service/testenv_integration_test.go`：`newEngineEnv`，按`cmd/contract-engine`的接线方式
  搭一整套引擎服务，加上造账户、造委托、造条件单的小工具。`restartEngine`会换一个全新的空订单簿、
  沿用数据库，用来测重启恢复。
- `internal/api/freeze_integration_test.go`：`newAPIEnv`，带真实仓库的API服务（鉴权关闭，
  `Kafka`换成替身），通过`httptest`直接调路由。

造委托的工具是 **直接落库**，不走下单接口，也不发事件——测引擎时由测试决定什么时候把委托交给
`SubmitOrder`，能精确构造"落库了但引擎还没处理"这类时序。

## 写新的集成测试

1. 文件顶部加`//go:build integration`
2. 用`newEngineEnv(t)`或`newAPIEnv(t)`拿夹具，不要自己建连接
3. 用`testutil.UIDBase()`加偏移生成uid，别写死
4. **写完做一次变异检查**：临时把被测的判断条件改坏（比如`if false && ...`），确认对应的测试会
   变红，再改回来。第一次跑就全绿的测试不代表它能抓到问题——账户冻结那组测试就靠这个发现过一个
   假阳性：条件单用市价单时，"因为冻结被撤销"和"没有对手盘被撤销"结果一样，关掉冻结检查测试照样通过，
   改成限价单才分辨得出来。

## 已经覆盖的和没覆盖的

**账户冻结**：仓库的状态变更事务和并发、引擎撮合前的冻结兜底（正常挂单、被撤销退款、平仓放行、
强平放行、不成交、失败关闭、重启恢复、条件单触发）、`POST /account/status`的清理逻辑和部分失败重试、
三个拦截点、幂等重放优先于冻结检查。

**资金操作**（`AccountRepo.ApplyFundOp`）：同一个`requestId`的重放与参数冲突、余额不足不占用
`requestId`、发信用额度只动`credit`、并发不超扣。其中"同一个`requestId`并发提交"是 **死锁回归测试**：
去掉账户行锁和死锁重试后，这个测试会稳定复现InnoDB的1213死锁错误。

**成交结算**：开仓成交后双方的余额、冻结保证金、仓位、maker/taker手续费流水、成交记录、标记价格；
平仓盈利后的余额、仓位归零、已实现盈亏流水；部分成交后撤销剩余部分按比例退保证金、重复撤单不重复退；
自成交保护。期望值都是按经济公式手算的，不是照代码输出抄的。

**结束本轮**（`EngineService.CloseRound`）：撤挂单和条件单并退保证金、按标记价强平仓位（盈亏、手续费、
保证金退回）、信用额度清零并记回收流水、投保复位、`round`+1、进度记录清理；过期和超前的`round`都被忽略，
已经结束的那一轮重复调用不会动下一轮的挂单和额度；缺标记价格时仓位保留、`round`不推进，补上后用同一个
`round`重试能完成。

**账户权益与强平**：权益口径（含仓位保证金和挂单冻结保证金，满仓开仓价格不动不会被强平的回归测试）、
触发边界（权益21.75 <= 维持保证金22.1触发，26.75不触发）、强平单在订单簿里按对手价成交、无对手盘时
超时兜底按标记价结算、缓冲扫入保险基金、穿仓由基金垫付（不动对手方）、基金不够时ADL强减盈利的对手方并刚好
补平缺口、还有仓位没平完时不提前结算、同一个uid的强平后结算串行（并发只垫付一次）、双仓位并发强平最终账目一致。
其中并发那两个测试去掉分布式锁会变红。强平时先撤掉全部挂单和条件单（含条件平仓单、不撤强平单和别的账户的单）：
触发时同步生效、结算前兜底再撤一遍；账户有挂单和条件单时基金垫付的是真实缺口5.75，不是被漏算后的705.75。

**资金费率结算**：正费率多头付空头收、负费率反过来（金额相等）、一个周期内多次采样取TWAP、费率封顶到±0.0075、
同一周期只结算一次且跨到下一周期才再结算、多个goroutine同时到点只转账一次（先占结算记录的唯一键再转账）、
缺指数价时采样被跳过、缺标记价格时推迟结算（补上后同一周期重试能结算）、已平仓的仓位不收资金费。
"同一周期已经结算过就提前返回"那个判断去掉后行为等价（`(symbol, funding_time)`唯一键才是真正的防重复），
所以变异检查里它是唯一存活的变异，不是测试缺口。

**K线聚合**：第一笔成交建出六个周期的K线且开盘时间按周期对齐、同一根里开盘取第一笔/最高最低取极值/收盘取
最后一笔/成交量和笔数累加、跨分钟跨整点时哪些周期另起一根哪些继续累加、50笔并发成交不丢、不同symbol隔离、
`FindRecent`只返回最近的且升序、一笔真实成交经引擎结算后六个周期都被更新。

**Kafka事件处理和消息去重**：不起Kafka，直接拿消息驱动`contract-engine`的三个处理函数
（`HandleOrderSubmit`/`HandleOrderCancel`/`HandleRoundClose`）：下单事件按库里的委托进订单簿且事件里的
价格数量symbol不可信、撤单事件摘单并退保证金且重复撤单不多退、结束本轮事件的`round`字段名与
`contract-api`发出的一致且过期`round`被忽略、找不到委托只记日志不算错误、损坏的消息返回错误。
`processed_messages`去重表：`TryMark`的坐标语义（偏移/分区/topic不同都是新消息、不同consumer group各自独立）、
16个并发只有1个赢、`DeleteOlderThan`清理后同一坐标可再标记；`WithDedup`接真实去重表时重复投递和并发重复
投递都只处理一次。

**市价单撮合**：市价买吃高于标记价的卖单、市价卖吃低于标记价的买单、市价平仓按买一成交，成交后保证金按真实成交额结算
（多冻的退、少冻的补），盘口不够吃时剩余量撤销并释放保证金；**价格保护带**：市价单的撮合价是参考价±保护带比例，共谋账户在
远离标记价处挂平仓限价单、受害者的市价买单不会被吃穿（钱原样退回），边界值（恰好+5%/-5%能成交，略超不能）、买卖两个方向、
带内成交带外撤销；端到端场景里也有一个真实的市价买入。变异检查：撤掉引擎里的价格保护，共谋和边界测试变红；买卖方向对调，
所有市价单测试变红。

**开仓买力**（`AccountService.FreezeMargin`的浮亏预检）：浮亏100时买力=346.75-100，冻结246.75成功、246.76被拒
（即使`available`有346.75）；已满足强平条件的账户任何新开仓都被拒；浮盈的行为不变；信用额度算买力且浮亏照扣；
没有仓位时不变；多个仓位按浮盈亏合计；通过真实的`POST /order/add`，浮亏账户下新单被拒、标记价回到没有浮亏时同样的
委托能成功。变异检查：去掉预检、预检不算信用额度、预检不扣浮亏，都会让对应测试变红；"浮盈时也做预检"是等价变异
（跟第3级的拒绝条件相同），不算测试缺口。

**模拟客户端**（`tools/sim-client`）的测试见 [sim-client.md](sim-client.md)：代理的签名和三道防护、做市的价格桶聚合和
差异计划、做市循环（假后端加假币安）。页面本身靠无头Chrome手工驱动验证，没有自动化测试。

没覆盖的：

- 条件开仓单创建后的"落库后再查一次冻结状态"：要制造"冻结刚好卡在检查和落库之间"的竞态，需要
  在代码里埋钩子，先没做
- Kafka broker的细节（分区重新分配的时序、消费位移提交）：端到端测试验证了"消费者能收到事件、重启后还活着"，
  但没有针对再平衡时序、位移提交的专项测试
- 消费者处理失败的消息不重试：`WithDedup`先标记再处理，处理出错的消息之后被重投也会被当成重复跳过，
  这是文档里已经写明的取舍（见 [message-dedup.md](message-dedup.md)），没有测试去钉住它
